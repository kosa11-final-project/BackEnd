<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockit.backend.feature.inventorysync.mapper.InventorySyncStateQueryMapper">
    <select id="selectSourceStates" resultType="com.stockit.backend.feature.inventorysync.vo.InventorySyncSourceStateVO">
        SELECT source_type, current_version, current_record_count, pending_record_count,
               last_changed_at, last_success_synced_at
          FROM inventory_source_state ORDER BY source_type
    </select>
    <select id="selectRunSources" resultType="com.stockit.backend.feature.inventorysync.vo.InventorySyncRunSourceVO">
        SELECT source_type, source_status, read_count, mapped_count, changed_count, error_count
          FROM inventory_sync_run_source WHERE inventory_sync_run_id = #{runId} ORDER BY source_type
    </select>
</mapper>
