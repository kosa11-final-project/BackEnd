<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockit.backend.feature.notification.mapper.NotificationMapper">
    <select id="selectRecentByUserId" resultType="com.stockit.backend.feature.notification.dto.NotificationResponse">
        SELECT notification_id,
               notification_type,
               severity,
               title,
               message,
               forecast_run_id,
               CASE WHEN read_yn = 'Y' THEN 1 ELSE 0 END AS read,
               created_at
        FROM notification
        WHERE user_id = #{userId}
          AND is_deleted = 0
        ORDER BY created_at DESC, notification_id DESC
        FETCH FIRST 100 ROWS ONLY
    </select>

    <select id="countUnreadByUserId" resultType="long">
        SELECT COUNT(*)
        FROM notification
        WHERE user_id = #{userId}
          AND read_yn = 'N'
          AND is_deleted = 0
    </select>

    <update id="markRead">
        UPDATE notification
        SET read_yn = 'Y',
            read_at = COALESCE(read_at, SYSTIMESTAMP),
            updated_at = SYSTIMESTAMP,
            updated_by = #{userId}
        WHERE notification_id = #{notificationId}
          AND user_id = #{userId}
          AND is_deleted = 0
    </update>
</mapper>
