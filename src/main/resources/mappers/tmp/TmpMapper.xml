<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stockit.backend.feature.tmp.mapper.TmpMapper">

    <select id="selectWelcomeMessage" resultType="TmpMessageVO">
        SELECT 1 AS id,
               'Stockit backend is running.' AS message_value
        FROM dual
    </select>

</mapper>
